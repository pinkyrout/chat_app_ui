import Types from '../actions/types';

const initialState = {
	messages: [],
	send: false
}

const chatBoxReducer = (state = initialState, action) => {
  switch(action.type){
    default:
      return state;
  }
};
